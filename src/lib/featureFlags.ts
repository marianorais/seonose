/**
 * Interruptores de funcionalidades ya implementadas que todavía no se muestran.
 * Para activarlas alcanza con poner el flag en `true` y desplegar; no hace
 * falta tocar los componentes.
 */

/**
 * Acceso al ranking desde la pantalla de resultados.
 * Ponelo en `true` cuando el ranking esté productivo.
 */
export const MOSTRAR_ACCESO_RANKING = false
